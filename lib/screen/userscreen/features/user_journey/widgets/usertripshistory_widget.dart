import 'package:flutter/material.dart';

class UserTripsHistoryWidget extends StatelessWidget {
  const UserTripsHistoryWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: SingleChildScrollView(
        child: Column(),
      ),
    );
  }
}
